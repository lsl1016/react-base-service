---
title: <模块名>模块功能文档
date: YYYY-MM-DD
version: vX.Y
type: system
module: <模块名>
maintainer: <维护人>
status: active
related_code:
  - router/<模块>.go
  - controllers/http/<模块>/
  - service/<模块>/
  - models/<模型>.go
summary: <模块>模块的完整功能说明
---

# <模块名>模块功能文档

## 1. 模块概述

功能定位与核心能力。2-4 句话说清这个模块解决什么问题、为谁服务。

若模块不涉及某类资源，在此显式说明（如"本模块不新增数据表，每次请求实时查询"）。

## 2. 接口清单

| 路径 | 方法 | 功能 | 控制器 |
|------|------|------|--------|
| `/api/xxx/list` | POST | 列表查询 | `xxx.List` |

若完整的请求/响应格式在独立的 API 文档中，在表格后加一行指向它，并说明职责边界。

## 3. 核心逻辑

只写有非平凡逻辑的部分。CRUD 样板不必赘述。

### 3.1 <功能点1>

### 3.2 <功能点2>

## 4. 数据模型

涉及的数据库表和关键字段。软删语义、状态枚举、跨模块共享的常量必须列出。

| 表 | 模型 | 说明 |
|------|------|------|

## 5. 配置项

相关配置说明。无新增配置时写"无新增配置"。

可按需改为"配置项与限制"，补充容量上限、层级限制等约束。

## 6. 历史版本

始终作为最后一节。新版本在上，倒序排列。

| 版本 | 日期 | 修改人 | 变更说明 |
|------|------|--------|----------|
| v1.0 | YYYY-MM-DD | <维护人> | 初始版本 |