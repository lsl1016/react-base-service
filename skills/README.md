# Repository Skills

- `bootstrap-project-docs/`：扫描代码并初始化/重构项目文档体系。
- `writing-changelog/`：代码变更后写 changelog，并在积累到阈值时整合进系统文档。

文档维护前先读 `docs/CONVENTION.md`。如需把这些 Skill 注册到运行时，可使用项目现有 `/skill/import` 或 `/skill/import_zip` 接口。